# Core Django (Essential)
Django==4.2.7
djangorestframework==3.14.0
django-environ==0.10.0

# Basic image processing (without compilation)
Pillow==10.0.1

# Security
cryptography==41.0.7

# Web server
whitenoise==6.6.0